const countryPopulation = 66500000;

if (countryPopulation > 33000000){
    console.log('UK population is above average');
} else {
    const averagePopulationNew = (countryPopulation - 22);
   console.log(`UK population is ${averagePopulationNew} below average`);
}