
const billValue = 275;

const tip = billValue <=300 && billValue >= 50 ? billValue * 0.15:
bill * 0.2;
console.log(`The bill was ${billValue}, the tip was ${tip} and the toal value is ${billValue + tip}`);